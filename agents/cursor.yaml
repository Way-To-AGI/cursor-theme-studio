# input: Skill 宿主读取
# output: 展示名与默认提示
# pos: agents 元数据
# 一旦我被更新，务必更新我的开头注释，以及所属的文件夹的 md。

interface:
  display_name: "Cursor Theme Studio"
  short_description: "多轮确认审美，生成并安全应用好看的 Cursor 主题"
  default_prompt: "Use $cursor-theme-studio to interview my taste (one or multi rounds), confirm a beautiful theme, then apply it in a QA Cursor window with try.mjs."
